{
  "type": "string",
  "minLength": 1,
  "pattern": "^[a-zA-Z0-9_-]+(\\/[a-zA-Z0-9_-]+)*$",
  "description": "Stable integration identity — slug segments joined by `/` (e.g. blueprint/app/api).",
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://blueprint.mzworthington.co.uk/schemas/v4/definitions/__schema1.schema.json"
}
