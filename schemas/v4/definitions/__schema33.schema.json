{
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "minLength": 1,
        "description": "Repository-relative path of the coupled file."
      },
      "score": {
        "type": "number",
        "description": "Temporal coupling score (0–1)."
      },
      "sharedCommits": {
        "type": "number",
        "description": "Commits where both files changed together."
      }
    },
    "required": [
      "path",
      "score",
      "sharedCommits"
    ],
    "additionalProperties": false
  },
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "https://blueprint.mzworthington.co.uk/schemas/v4/definitions/__schema33.schema.json"
}
