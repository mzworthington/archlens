/** Latest GitHub release - manual install fallback. */
export const CLI_RELEASES_URL = 'https://github.com/mzworthington/archlens/releases/latest';

/** In-app install walkthrough (install script, verify, scan). */
export const CLI_GETTING_STARTED_PATH = '/guide/getting-started';

/** One-line install script shown in onboarding UI and docs CTAs. */
export const CLI_INSTALL_COMMAND =
  'curl -fsSL https://raw.githubusercontent.com/mzworthington/archlens/main/scripts/install.sh | sh';

/** Example headless scan command shown in onboarding UI. */
export const CLI_SCAN_COMMAND = 'archlens --headless --output=blueprints';
