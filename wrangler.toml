# Cloudflare Pages — static deploy of the Canvas SPA (build happens in CI).
# Local preview: cd app && pnpm build && npx wrangler pages dev app/packages/canvas/dist
name = "archlens"
compatibility_date = "2026-08-04"
pages_build_output_dir = "app/packages/canvas/dist"
