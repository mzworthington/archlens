# Cloudflare Pages — static deploy of the designer SPA (build happens in CI).
# Local preview: cd app && pnpm build && npx wrangler pages dev app/packages/designer/dist
name = "archlens"
compatibility_date = "2024-01-01"
pages_build_output_dir = "app/packages/designer/dist"
