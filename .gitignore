# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
coverage
site
*.local
# pnpm local store
.pnpm-store/

# Environment & secrets (`.env.development` is a committed non-secret default)
.env
.env.*
!.env.example
!**/.env.example
!.env.development
!**/.env.development
*.pem
*.key
secrets.json
credentials.json



# Mirrored from repo samples/ at designer build/dev time (bundled demo workspace)
app/packages/canvas/public/bundled-blueprints/

# Mirrored from repo chaos-specs/ at designer build/dev time (ChaosSpec catalog picker)
app/packages/canvas/public/bundled-chaos-specs/

# Synced from repo schemas/ at designer build/dev time (hosted for IDE validation)
app/packages/canvas/public/schemas/

# Built from resilience-engine/ via make copy-wasm (CI + local dev)
app/packages/canvas/public/resilience-engine/chaoslens.wasm
app/packages/canvas/public/resilience-engine/wasm_exec.js

# Copied from tree-sitter-wasms at designer build/dev time (syntax highlighting)
app/packages/canvas/public/tree-sitter/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/mcp.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.tsbuildinfo

# Pulumi (infra/cloudflare)
infra/cloudflare/node_modules/
infra/cloudflare/bin/
infra/cloudflare/.pulumi/
.pulumi/
# Stack config may contain account IDs and encrypted API tokens after bootstrap
infra/cloudflare/Pulumi.*.yaml
!infra/cloudflare/Pulumi.prod.yaml.example
!infra/cloudflare/Pulumi.yaml


playwright-report/
test-results/
lighthouse-report/
.lighthouseci/

# VHS demo scratch output (GIF lands in docs/screenshots/)
.vhs*/
docs/screenshots/.record-scratch/
# Wrangler local state (`wrangler dev`, Durable Object persistence, bundle tmp)
.wrangler/
**/.wrangler/

# Local git worktrees and scratch patches (never commit)
.worktrees/

# Antigravity workspace MCP (wk mcp --project / wk init)
.agents/mcp_config.json

# wk mcp backups of host MCP files
**/*.bak.*
# Waykit session MCP profile name (names only)
.wk-mcp-profile.stamp
