# yaml-language-server: $schema=https://archlens.dev/schemas/latest/chaos.schema.json
version: https://archlens.dev/schemas/v1/chaos.schema.json

metadata:
  name: External scope auth dependency outage
  description: Fault the external auth proxy referenced from the scope diagram — requires workspace materialization in ArchLens Canvas; validates cross-diagram dependency risk in CLI estate sweeps.
  diagramRef: chaoslens-stress/external-scope

faults:
  - nodeId: chaoslens-stress/external-auth/auth
    faultType: region-outage
